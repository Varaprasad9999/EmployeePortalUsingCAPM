FUNCTION "FN_FINAL_NON_WEEK"(STARTDATE DATE, ENDDATE DATE)
RETURNS TABLE(STARTDATE DATE, ENDDATE DATE, NONWEEKDAYS INTEGER)
LANGUAGE SQLSCRIPT 
SQL SECURITY INVOKER AS

BEGIN

RETURN

SELECT STARTDATE, ENDDATE, (FN_NONWEEKSCALAR(STARTDATE, ENDDATE)) AS NONWEEKDAYS FROM DATA_LEAVES;

END;
